版本号x.y.z ：其中z表示一些小的bugfix，更改z的号，

y表示一些大的版本改动，比如一些API的变化，

x表示一些设计的变动以及模块的重构之类的，会升级x版本号

在package.json里面dependencies依赖包的版本号前面的符号有两种，一种是~，一种是^。

~的意思是匹配最近的小版本 比如~1.0.2将会匹配1.0.X版本，但不会匹配1.1.0；

^的意思是配备最近的一个大版本 比如 ^1.0.2将会匹配所有的1.X.X,但不包括2.X.X;